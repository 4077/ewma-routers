<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="routers">
        <!-- router -->
        <div class="router {ENABLED_CLASS}" router_id="{ID}" hover="hover">
            <div class="buttons">
                {RENAME_BUTTON}
                {TOGGLE_ENABLED_BUTTON}
                {EXCHANGE_BUTTON}
                {DUPLICATE_BUTTON}
                {DELETE_BUTTON}
                <div class="cb"></div>
            </div>

            <div class="name">
                {NAME}
            </div>
        </div>
        <!-- / -->
    </div>

    <div class="buttons">
        {CREATE_BUTTON}
        {COMPILE_BUTTON}
    </div>

</div>

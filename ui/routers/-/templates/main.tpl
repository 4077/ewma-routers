<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="cp">
        {CREATE_BUTTON}
        {COMPILE_BUTTON}
    </div>

    <div class="routers">
        <!-- router -->
        <div class="router {SELECTED_CLASS}" router_id="{ID}">
            {CONTENT}
        </div>
        <!-- / -->
    </div>

</div>

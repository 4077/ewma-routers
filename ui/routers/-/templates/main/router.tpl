<div class="{__NODE_ID__} {DISABLED_CLASS} {SELECTED_CLASS}" instance="{__INSTANCE__}">

    <div class="buttons">
        {TOGGLE_ENABLED_BUTTON}
        {RENAME_BUTTON}
        {EXCHANGE_BUTTON}
        {DUPLICATE_BUTTON}
        {DELETE_BUTTON}
        <div class="cb"></div>
    </div>
    <div class="name">{NAME}</div>
    <div class="cb"></div>
    <div class="hosts">
        <!-- host -->
        <div class="host">
            {NAME}
        </div>
        <!-- / -->
    </div>

</div>
